# singbox TUI 产品设计计划

## 2026-09-17 — Surge-style 产品收敛与 Activity 分流（当前）

用户最新指示取代 v1 规格中“五个主工作区”的导航约定：顶部只保留 Overview / Policies（分组与分流）/ Activity；Start / Mode / TUN 作为稳定底部控制；低频和原生配置进入 Config，其组织尽量跟随 sing-box 配置顶层结构。

- [x] 盘点当前 `surge-style-redesign` WIP 与未提交的共享编辑器；不覆盖用户现有改动。
- [x] 修复当前编译基线，完成三工作区与减法后底栏；保留所有核心能力的可达路径。
- [x] 完成 Policies：组选择/测速/编辑，规则顺序与编辑，订阅/规则来源入口；复用同一原生对象编辑语言。
- [x] 完成 Activity：Connections / Apps / Logs，按时间或流量排序，从连接或已观测进程快速生成域名、后缀、正则、进程或 App+Domain 路由规则。
- [x] 完成 Config：Core 信息/选择/下载，并按 log / dns / ntp / certificate / endpoints / inbounds / outbounds / route / services / experimental 结构编辑；DNS 使用同一列表+详情编辑语言。
- [ ] 增加窄屏、键盘、快速规则、排序、原生保真回归；运行 fmt / test / clippy / release / 离线 preview，分阶段 Git 提交。

边界：Activity 的 App 列表以 sing-box 实际观测到的网络连接为事实来源，不冒充 Little Snitch 的系统级完整进程库。开发验证不启停用户真实核心、不改真实系统代理/TUN，不自动推送。

## 2026-09-16 — Overview 与 Actions 收敛（当前）

用户已批准实施：不加出口摘要；保留主页分组快捷选择，增加共享 Connections 小窗、实时流量/连接检查/接管证据；去掉无子页的空行；移除所有 : More，仅保留分类去重的底部 : Actions；区分首次引导和日常动作。

- [ ] 实现状态/连接数据刷新、按 ID 稳定浏览与详情复用；后台轮询不截获按键、不消费编辑事务。
- [ ] 实现自适应首页、分组滚动、连接窗聚焦、主操作整理，以及唯一分类 Actions 菜单。
- [ ] 单元/布局/隔离连接验证、宽窄画面及 PTY、文档同步、最终构建。保留原有本地改动，不启停真实网络，不自动推送。

## 2026-09-16 — 界面美学与操作减负（当前）

用户认可功能归属，授权重新考虑视觉与操作。不改五工作区、原生配置语义或网络行为；直接修改正式客户端，不做独立原型，不自动推送或发布。

- [x] 实走 80×24 Demo，审视焦点路径与视觉层级，查阅 Yazi / Lazygit / Harlequin 官方资料。
- [x] 正式实现：上方状态/导航、底部带键位的全局操作、局部 Tab、克制的页面层级与上下文记忆。
- [x] 补充键盘/布局回归，检查窄宽终端和输入隔离，构建并交付本地可运行版本。129 普通测试、Clippy、1 项显式离线渲染与 80×24 PTY 通过；版本 0.6.1-dev 未公开发布，待用户审美/操作反馈。

交互修订：取代 v1 规格中“全局按钮在顶部、Tab 穿过所有导航区域”的具体布局约定；保留可见任务入口、显式 Review/Apply 与所有安全确认。Tab 只在内容/页面动作间切换；F6 直达底部控制；数字和方括号切页，文本输入时不触发全局命令。

## 2026-09-16 — GitHub 公开发布与 README

- [x] 核对 GitHub 身份 mmei0114；用户确认 sing / MIT，检查公开范围与 4 commits / 77 blobs 历史敏感模式，未见高置信度匹配或运行状态路径。
- [x] 参考成熟开源 README，完成英文首页、中文入口、真实 Demo SVG、源码安装、独立读者复核、贡献/安全说明与详细手册；本地 122 测试、格式和 Clippy 通过。
- [x] 验证链接、命令、读者理解和代码回归；公开仓库 mmei0114/sing 已推送 main，MIT/14 topics/私密漏洞报告及公开 README 已核对。CI 35065643935 的 macOS/Linux 检查与 release 构建全部通过；v0.6.0 按源码公开预览版发布，不上传未签名二进制。

发布边界：不上传真实订阅、私有运行数据或本地构建缓存；不冒称 Linux/真实接管已验收，不承诺搜索排名；不向外部社区批量投递或购买推广。

## 当前唯一实施方向：正式客户端产品化 v1

权威规格：docs/product-spec-v1.md。取代下方历史六页/侧栏/独立原型建议。每次恢复工作先读规格、本文和 progress.md。

产品内实施目标已创建，尚未完成；目标正文引用本规格及 M1–M4、A01–A18。此处不另设 token 预算。

- [x] M0 固化五工作区、子页面归属、三主流程、原生保真与验收标准；本轮仅设计落盘，不代表实现完成。
- [x] M1 五工作区与统一交互：Overview / Proxies / Routing / Network / Activity；顶部导航、全局 Settings、可见按钮、列表/详情、窄屏键盘路径。证据：78 项普通测试、Clippy、80×24 离线 PTY；详见 progress.md。完整用户流程尚属 M2，不能将此项等同 A02–A18 全部通过。
- [x] M2 三条主流程的代码与隔离端口验证完成；真实接管和无指导用户验收仍按 M4 单列。
  - 原生 local/remote Source/SRS 已统一到 F3 父事务，保留类型与核心更新归属；F1/F2/F3、Update All、User-Agent、错误恢复、订阅 Refresh Preview 与规则 Change Source 上下文重准备均已落地。真实 core 加载 SRS 通过，公开 remote 获取尚未验收。
- [ ] M3 原生对象深入编辑、引用跳转、语义 Review、准确运行证据和恢复反馈。
  - 原生引用图、全局语义 Review/脱敏 Native Diff、API 回读选择、独立选择记忆与恢复已实现并回归。客户端 Global target 等非原生引用导航仍未覆盖；核心缓存/实际主机异常恢复的完整验收待补，不能把原生引用图当作所有依赖。
- [ ] M4 全量回归/正式构建/平台范围记录，并完成规格 A01–A18 的验收和用户实际任务测试。
  - 0.6.0 本地发布、122 项普通测试、9 项显式隔离/只读验证及平台范围台账见 docs/acceptance-0.6.0.md；A03/A18、真实 System/TUN/Linux/SSH 仍待验收。旧 0.5.1 二进制已单独备份，不自动重启用户实例。

执行边界：英文 UI，直接做正式代码，不做独立原型；不重写原生底座，不擅自启停用户核心或改真实网络。缺少真实授权/用户实测时记录未验收项，不能只因构建通过就宣称目标完成。

## 产品交互重新研究（本轮仅研究与方案）

- [x] 收集官方 sing-box 客户端、Surge、Clash Verge Rev、QX 的官方界面/文档/源码，以及优秀 TUI 交互素材。
- [x] 对比首次上手、节点/策略选择、订阅/规则、配置和诊断的组织方式；区分观察事实与本产品建议。
- [x] 输出可回看的素材索引、产品结构与关键流程方案、验收标准，先与用户确认，不继续修改 UI 代码。

历史产物：docs/product-research-2026-09-15.md。研究完成；其中六页结构与交互原型建议已被 docs/product-spec-v1.md 取代。本研究没有实施 UI、修改用户配置或操作真实网络。

## 0.5.1 导航和操作可发现性

- [x] 恢复顶部导航和数字直达，取消侧栏；Advanced 不再重复已有专页对象。
- [x] 页面常用操作就地提示，显式建组和导入入口；表单/成员选择提示准确。
- [x] 渲染、快捷键和数据保真回归，离线终端检查、release 构建与版本确认；源码提交见本地 Git。

只调整界面与操作入口，不重启用户后台、不修改用户配置或网络。辅助技能路径缺失，继续现有三文件记录。

## 0.5 原生配置重构（2026-09-15，用户已批准）

- [x] Git 基线 d45b9be / v0.4.1-baseline，升级分支 native-config-redesign；.build/backups/v0.4.1/sing 可运行备份与 SHA-256 已核对。
- [x] 原生文档、一次性确认迁移、私有备份、局部编辑与并发版本检查；订阅只更新拥有的对象。
- [x] 英文新导航与统一对象操作，DNS/入站/出站/路由表单、原生编辑入口、独立 Review/Apply。
- [x] 原生依赖和管理接口校验、运行时组选择、隔离核心回归、终端检查与文档。
- [x] 最终 release 构建和版本确认；最终源码提交记录见 Git 分支。

验收：70 项普通测试、8 项显式隔离/只读测试与 Clippy 全目标零警告通过。真实 1.14.0 内核验证迁移/分组/规则转换/草稿与运行分离/应用与回滚，以及八类 DNS 模板的配置语法；不包含实际 DNS 可达性与速度、真实 TUN 或 Linux/SSH 网络验收。后续能力见 README，不把本阶段等同于完整长期路线图。

执行边界：不重启用户当前实例、不改主机代理；迁移由新版客户端内显式确认后执行。源码与运行数据分离。指定辅助技能路径缺失，继续三文件记录。

## 2026-09-15 全局架构与交互反思（历史讨论，已由上方实施记录接续）

- [x] 只读审视配置生成器、存储模型、订阅/规则转换与八页导航；核对官方原生能力及平台边界。
- [x] 提出原生配置保真、配置对象导航、运行观察分层、英文单语言和渐进说明的讨论方案。
- [ ] 用户确认方向后，先审计 Git 排除项，建立当前 0.4.1 源码基线与独立可运行备份，再进行升级。
- [ ] 确认后分阶段实施：原生配置与事务模型 → 配套导航和对象编辑 → DNS/路由/入出站闭环 → 平台与故障验收。

本轮仅更新三文件讨论记录，不修改代码、用户配置或网络，不初始化 Git。用户明确要求先敲定方向；不把讨论建议视作已批准规格。Superpowers 与 planning-with-files 指定路径仍缺失。

## 0.4.1 用户试用修复（当前）

- [x] 只读查看实际启动错误，隔离复现 Rule 失败，修复并增加回归。
- [x] 明确主页显示的是模式目标与组内实际节点，不再使用含糊 Proxy choice。
- [x] 分离中英文设置/新页面文案，跟随语言设置；检查交互回归并构建。

0.4.1 交付：70项普通测试、7项显式本地/只读测试、格式检查、Clippy零警告、release通过；PTY确认0.4.1、中英文M独立显示、L切换、中文D诊断和正常退出。未启用或切换真实TUN；此次只读发现用户正在尝试TUN，核心DNS修复已在隔离端口模式验收。

不重启用户管理器、不切换主机代理；凭据不输出、不复制到测试。指定两个辅助技能路径仍缺失，沿用三文件记录。

## 0.4：专业控制与可解释诊断（当前）

- [x] 独立路由模式：规则 / 全部代理 / 直连，明确内网例外、DNS 配合、保存与应用状态；兼容旧配置。
- [x] 原生连接观察与单连接关闭：实际目标、节点链、流量、进程信息（可用时），不把预测冒充实际命中。
- [x] TUI 分层配置与诊断：接管 / 路由 / DNS 清楚区分，DNS 生效边界、TUN 实验与 SSH 风险可见。
- [x] 隔离核心 / 路由模式 / 连接 API 集成、交互回归、文档及 release。

0.4.0 交付：66项普通测试、7项显式本地/只读测试通过；格式、Clippy零警告和release通过。PTY离线验证M保存、8连接详情/关闭确认取消、D诊断和q恢复终端；长诊断页底栏重叠已修复并加入宽/窄终端回归。升级由用户主动关闭旧后台，不影响本轮测试期间的真实连接。

用户意图按“成熟专业且不失易用性”理解。优先交付上述完整闭环；高级 TUN 平台验收、App 自动选择器与自动更新等仍需后续专门实现与验证，不宣称一次性覆盖所有内核能力。不启停用户当前连接或改主机网络。指定 Superpowers / planning-with-files 路径仍不存在，继续本项目三文件记录。

## 0.3 实现：分组、远程规则与配套 DNS

- [x] 兼容旧存储的分组、规则资源及绑定模型；严格转换器与测试。
- [x] 原生配置生成、DNS 配套、后台管理与安全的更新预览。
- [x] Groups / Rules TUI、节点勾选、目标绑定、顺序与警告。
- [x] 真实核心与回环集成、终端验证、文档和 release。

交付 0.3.0：57 项普通测试、6 项显式本地 / 只读测试通过，Clippy 零警告，release 和离线 PTY 验证通过。公开 YouTube URL 直连下载失败，不计作在线验收；未动用户真实连接和系统设置。复杂规则 / 二进制格式、自动刷新与逐应用验收留待后续。

本轮不启停用户连接、不改系统代理；使用独立临时目录与虚构节点。旧 DNS 行为保留，配套 DNS 为可选模式。

## 远程分类规则集澄清与规范化建议

- [x] 阅读用户提供的 blackmatrix7 QX YouTube.list 实际内容。
- [x] 核实常见匹配转换边界、原生 rule-set 和 provider/策略分离做法。
- [x] 区分配置管理与实时转发流程，提出规则订阅、规则集、策略组、路由绑定及 DNS 配套设计。
- 本轮是讨论，不实现转换器；用户明确目标为分类规则远程订阅，不是完整 QX/Clash 配置导入。

## 分组、跨格式规则、进程与 DNS：设计讨论

- [x] 核实 selector / URLTest、rule-set、进程路由及 DNS 原生边界。
- [x] 对照 Clash/Mihomo 与 QX 官方规则示例，区分匹配条件、动作和完整配置。
- [x] 提出新手工作流、兼容策略、关键安全边界和分阶段实现建议。
- 本轮为解释与方案建议，不新增分组或转换器代码，不修改网络。现有 0.2 仍只有单 proxy selector 与简单手工规则。

## 2026-09-14 试用反馈：只读诊断

- [x] 检查系统接管提示的代码条件和当前脱敏运行状态。
- [x] 核查配置与官方性能相关字段，区分事实和待测假设。
- [x] 说明提示的状态混淆、当前无分流以及进一步对比所需信息。
- 用户补充：同一台电脑、同一个具体节点，网页首开慢且视频卡顿；希望先讨论 DNS 等尚不完整的配置设计。网络 / 浏览器 / 接管方式是否相同及连接时提示尚待复核。本轮不修改程序或代理配置。

当前任务：实现 macOS 系统代理的启用、原设置备份、安全恢复、异常监护和准确状态显示。使用模拟后端测试，实机写入需另行确认。

## 系统代理阶段

- [x] 实现可注入的代理事务与持久恢复记录，测试部分失败和外部修改冲突。
- [x] macOS 原生配置接口、显式授权 helper、后台故障监护。
- [x] 接入连接/断开、恢复入口、准确状态和独立连通性检查。
- [x] 完成模拟、只读原生与既有核心回归测试，构建并交付；不擅自实机启用。

交付：sing 0.2.0 release 已构建；44 项普通测试、5 项显式回环 / 只读验证、Clippy 零警告通过。管理员真实写入与浏览器端到端验收尚未进行，需要用户在本机确认授权；不计作已通过。

## 本轮

- [x] 只读检查运行模式与 macOS 代理设置。
- [x] 更新程序、命令、文档及测试中的名称，兼容旧数据目录。
- [x] 28 项回归测试、Clippy 和 release 构建；新命令只读确认旧数据兼容。解释诊断证据和未验证部分。

## 已确定的约束

- 真正的全屏 TUI，在界面内管理订阅和配置。
- 普通用户只需粘贴常见订阅链接，无需先填写配置文件或执行提取流水线。
- fly-auto-singbox 仅作订阅识别机制参考，不继承其流程、地区限制或整体架构。
- 希望获得接近日常代理客户端的易用性；探索使用 sing-box 1.14+ 官方管理 API。

## 阶段

- [x] 了解当前工作区：空目录，无现有实现。
- [x] 查阅优秀 TUI 的官方交互资料。
- [x] 提出产品结构、核心流程、交互规则和状态设计。
- [x] 编写初版设计与可验证的验收标准。
- [x] 接收用户偏好：Mac 本机 + Linux 本机及 SSH；英文优先，保留中文切换。
- [x] 按反馈收敛第一版边界；用户授权实现。

## 实现阶段

- [x] Rust/Ratatui 工程、订阅解析、私有存储、配置生成与测试。
- [x] 后台管理与官方 gRPC 适配，核心安装、校验、启动与停止；端口模式真实核心验证通过。
- [x] 原生 TUI，导入、节点、订阅、配置辅助、活动页面；英文优先，中文导航与帮助可切换。
- [x] macOS 终端交互、真实核心和故障路径验证，使用文档及 release 可运行交付。

第一版明确保留的后续工作：Linux / SSH / TUN 真机验证、完整中文诊断、自动更新、开机自启、连接列表、规则集管理与任意 JSON 高级编辑。README 已区分实现状态与设计目标。

## 限制

用户指定的 planning-with-files 技能路径当前不存在；已搜索可用 skills 目录，未找到。按用户明确给出的 task_plan/findings/progress 三文件规范记录。
