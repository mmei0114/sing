# singbox TUI 产品设计计划

## 2026-09-15 全局架构与交互反思（方向待确认）

- [x] 只读审视配置生成器、存储模型、订阅/规则转换与八页导航；核对官方原生能力及平台边界。
- [x] 提出原生配置保真、配置对象导航、运行观察分层、英文单语言和渐进说明的讨论方案。
- [ ] 用户确认方向后，先审计 Git 排除项，建立当前 0.4.1 源码基线与独立可运行备份，再进行升级。
- [ ] 确认后分阶段实施：原生配置与事务模型 → 配套导航和对象编辑 → DNS/路由/入出站闭环 → 平台与故障验收。

本轮仅更新三文件讨论记录，不修改代码、用户配置或网络，不初始化 Git。用户明确要求先敲定方向；不把讨论建议视作已批准规格。Superpowers 与 planning-with-files 指定路径仍缺失。

## 0.4.1 用户试用修复（当前）

- [x] 只读查看实际启动错误，隔离复现 Rule 失败，修复并增加回归。
- [x] 明确主页显示的是模式目标与组内实际节点，不再使用含糊 Proxy choice。
- [x] 分离中英文设置/新页面文案，跟随语言设置；检查交互回归并构建。

0.4.1 交付：70项普通测试、7项显式本地/只读测试、格式检查、Clippy零警告、release通过；PTY确认0.4.1、中英文M独立显示、L切换、中文D诊断和正常退出。未启用或切换真实TUN；此次只读发现用户正在尝试TUN，核心DNS修复已在隔离端口模式验收。

不重启用户管理器、不切换主机代理；凭据不输出、不复制到测试。指定两个辅助技能路径仍缺失，沿用三文件记录。

## 0.4：专业控制与可解释诊断（当前）

- [x] 独立路由模式：规则 / 全部代理 / 直连，明确内网例外、DNS 配合、保存与应用状态；兼容旧配置。
- [x] 原生连接观察与单连接关闭：实际目标、节点链、流量、进程信息（可用时），不把预测冒充实际命中。
- [x] TUI 分层配置与诊断：接管 / 路由 / DNS 清楚区分，DNS 生效边界、TUN 实验与 SSH 风险可见。
- [x] 隔离核心 / 路由模式 / 连接 API 集成、交互回归、文档及 release。

0.4.0 交付：66项普通测试、7项显式本地/只读测试通过；格式、Clippy零警告和release通过。PTY离线验证M保存、8连接详情/关闭确认取消、D诊断和q恢复终端；长诊断页底栏重叠已修复并加入宽/窄终端回归。升级由用户主动关闭旧后台，不影响本轮测试期间的真实连接。

用户意图按“成熟专业且不失易用性”理解。优先交付上述完整闭环；高级 TUN 平台验收、App 自动选择器与自动更新等仍需后续专门实现与验证，不宣称一次性覆盖所有内核能力。不启停用户当前连接或改主机网络。指定 Superpowers / planning-with-files 路径仍不存在，继续本项目三文件记录。

## 0.3 实现：分组、远程规则与配套 DNS

- [x] 兼容旧存储的分组、规则资源及绑定模型；严格转换器与测试。
- [x] 原生配置生成、DNS 配套、后台管理与安全的更新预览。
- [x] Groups / Rules TUI、节点勾选、目标绑定、顺序与警告。
- [x] 真实核心与回环集成、终端验证、文档和 release。

交付 0.3.0：57 项普通测试、6 项显式本地 / 只读测试通过，Clippy 零警告，release 和离线 PTY 验证通过。公开 YouTube URL 直连下载失败，不计作在线验收；未动用户真实连接和系统设置。复杂规则 / 二进制格式、自动刷新与逐应用验收留待后续。

本轮不启停用户连接、不改系统代理；使用独立临时目录与虚构节点。旧 DNS 行为保留，配套 DNS 为可选模式。

## 远程分类规则集澄清与规范化建议

- [x] 阅读用户提供的 blackmatrix7 QX YouTube.list 实际内容。
- [x] 核实常见匹配转换边界、原生 rule-set 和 provider/策略分离做法。
- [x] 区分配置管理与实时转发流程，提出规则订阅、规则集、策略组、路由绑定及 DNS 配套设计。
- 本轮是讨论，不实现转换器；用户明确目标为分类规则远程订阅，不是完整 QX/Clash 配置导入。

## 分组、跨格式规则、进程与 DNS：设计讨论

- [x] 核实 selector / URLTest、rule-set、进程路由及 DNS 原生边界。
- [x] 对照 Clash/Mihomo 与 QX 官方规则示例，区分匹配条件、动作和完整配置。
- [x] 提出新手工作流、兼容策略、关键安全边界和分阶段实现建议。
- 本轮为解释与方案建议，不新增分组或转换器代码，不修改网络。现有 0.2 仍只有单 proxy selector 与简单手工规则。

## 2026-09-14 试用反馈：只读诊断

- [x] 检查系统接管提示的代码条件和当前脱敏运行状态。
- [x] 核查配置与官方性能相关字段，区分事实和待测假设。
- [x] 说明提示的状态混淆、当前无分流以及进一步对比所需信息。
- 用户补充：同一台电脑、同一个具体节点，网页首开慢且视频卡顿；希望先讨论 DNS 等尚不完整的配置设计。网络 / 浏览器 / 接管方式是否相同及连接时提示尚待复核。本轮不修改程序或代理配置。

当前任务：实现 macOS 系统代理的启用、原设置备份、安全恢复、异常监护和准确状态显示。使用模拟后端测试，实机写入需另行确认。

## 系统代理阶段

- [x] 实现可注入的代理事务与持久恢复记录，测试部分失败和外部修改冲突。
- [x] macOS 原生配置接口、显式授权 helper、后台故障监护。
- [x] 接入连接/断开、恢复入口、准确状态和独立连通性检查。
- [x] 完成模拟、只读原生与既有核心回归测试，构建并交付；不擅自实机启用。

交付：sing 0.2.0 release 已构建；44 项普通测试、5 项显式回环 / 只读验证、Clippy 零警告通过。管理员真实写入与浏览器端到端验收尚未进行，需要用户在本机确认授权；不计作已通过。

## 本轮

- [x] 只读检查运行模式与 macOS 代理设置。
- [x] 更新程序、命令、文档及测试中的名称，兼容旧数据目录。
- [x] 28 项回归测试、Clippy 和 release 构建；新命令只读确认旧数据兼容。解释诊断证据和未验证部分。

## 已确定的约束

- 真正的全屏 TUI，在界面内管理订阅和配置。
- 普通用户只需粘贴常见订阅链接，无需先填写配置文件或执行提取流水线。
- fly-auto-singbox 仅作订阅识别机制参考，不继承其流程、地区限制或整体架构。
- 希望获得接近日常代理客户端的易用性；探索使用 sing-box 1.14+ 官方管理 API。

## 阶段

- [x] 了解当前工作区：空目录，无现有实现。
- [x] 查阅优秀 TUI 的官方交互资料。
- [x] 提出产品结构、核心流程、交互规则和状态设计。
- [x] 编写初版设计与可验证的验收标准。
- [x] 接收用户偏好：Mac 本机 + Linux 本机及 SSH；英文优先，保留中文切换。
- [x] 按反馈收敛第一版边界；用户授权实现。

## 实现阶段

- [x] Rust/Ratatui 工程、订阅解析、私有存储、配置生成与测试。
- [x] 后台管理与官方 gRPC 适配，核心安装、校验、启动与停止；端口模式真实核心验证通过。
- [x] 原生 TUI，导入、节点、订阅、配置辅助、活动页面；英文优先，中文导航与帮助可切换。
- [x] macOS 终端交互、真实核心和故障路径验证，使用文档及 release 可运行交付。

第一版明确保留的后续工作：Linux / SSH / TUN 真机验证、完整中文诊断、自动更新、开机自启、连接列表、规则集管理与任意 JSON 高级编辑。README 已区分实现状态与设计目标。

## 限制

用户指定的 planning-with-files 技能路径当前不存在；已搜索可用 skills 目录，未找到。按用户明确给出的 task_plan/findings/progress 三文件规范记录。
