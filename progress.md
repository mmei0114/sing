# 进度

## 2026-09-16 — Overview 与 Actions 收敛

- 用户确认不展示出口摘要，保留组快捷选择，增加小型连接窗口，合并重复命令入口并整理首页。
- 已读取既有实现与产品规格；开始正式代码修改。沿用 frontend-design 的层级/留白原则；两个指定辅助路径仍缺失。
- 现有 0.6.1-dev 未提交改动属于上一轮用户认可版本，保留并在其上升级；不操作真实代理或用户订阅。

## 2026-09-16 — 界面美学与操作减负

- 已检查实际焦点路径、页面渲染与 80×24 离线 Demo；确定不改功能归属，重做界面层级和操作框架。
- frontend-design 技能用于留白、层级、强调色与可用性；Superpowers / planning-with-files 指定文件仍缺失，沿用现有三文件记录。
- 已实现 0.6.1-dev：底栏带键位的 Start/Mode/Settings/Review/Help/Actions，Tab 局部循环、F6 控制栏；I 全局订阅导入、C 路由规则导入；保留所有停止/应用确认与输入优先级。
- 减少嵌套边框，统一状态/事实对齐与薄荷强调色；组行直接显示成员数及 Current/Default，DNS 页签与编辑动作分离，弹窗居中/紧凑并淡化背景；没有真实 API 证据时不显示实时流量或已知当前组成员。
- page/tab 独立保存过滤与选择；回到删除后的行会夹到合法范围。引用跳转清除目标页旧过滤以保证引用对象可见，原上下文仍由引用历史恢复。
- 129 项普通测试、fmt、全目标 Clippy 零警告通过；新增测试覆盖局部焦点、快捷键输入隔离、DNS 页签、上下文记忆、底栏锚定、恢复/API 失效状态、250 个含中文名称的组及模态快捷键提示。另显式执行 1 项离线 SVG 渲染测试，检查 54×18 / 80×24 / 120×35 的真实 renderer 虚构数据画面；图像在 .build/ui-review，不含真实节点或订阅。
- v0.6.0 二进制备份：.build/backups/v0.6.0-5302560/sing；SHA-256 cfb36465b557e7263d6d15607a89212daad73b70df8950122e95f69d97d2b07f。管理协议仍为 9，不要求 shutdown；本轮不重启真实管理器，不改网络，不推送/发布。
- 最终 release 构建完成，./sing --version 为 0.6.1-dev；SHA-256 f7f3c2546284369a8cb85b2d8ee50cae61f6f95c2b06581af09a2a6d5cdf7431。80×24 PTY 实走 Tab→订阅导入→Esc、逗号 Settings、4/方括号到 DNS、左右 DNS 页签、F6 底栏、Esc 返回及 q 正常恢复终端通过。
- README 预览 SVG 已从最终二进制重生成（布局来自真实 renderer，文档 SVG 颜色做简化）；43 个本地链接检查和 git diff --check 通过。最终源码尚未提交/推送；验收未涉及真实 System/TUN/Linux/SSH 网络，审美与用户实际操作评价留给本地试用。

## 2026-09-16 — GitHub 首次公开发布准备

- 收尾验收：CI 35065643935（代码提交 02bc6b9）macOS/Linux 全绿，均完成 fmt、普通测试、Clippy -D warnings、release、version/preview 和本地文档链接验证。本地复跑 122 普通 / 9 隔离只读测试通过；新版本地二进制仍为 0.6.0，SHA-256 cfb36465b557e7263d6d15607a89212daad73b70df8950122e95f69d97d2b07f；重新生成 SVG 与已发布内容完全一致。
- 独立新读者再次复核 README/手册后未见阻塞理解的问题。最终文档仅补 CI 证据与发行记录，不再更改受验代码；v0.6.0 采用源码公开预览发布，标签与 Release 以 GitHub 记录为准。真实网络接管、Linux/SSH 和无指导用户任务仍未全部验收，不改变长期产品目标状态。搜索排名和自然流量不作保证，未向第三方社区投递广告。

- 源码与文档已推送 main（680982e），GitHub 公开页、MIT 识别、14 个 topics 和私密漏洞报告 enabled 已读取确认。首个远端 CI（35046449449）macOS 通过；Linux 普通测试通过，但 Clippy 揭示 macOS 专用事务/Watchdog 在 Linux 生产构建中未使用。
- 发布前修复将事务实现拆入 cfg(macos 或 test) 的 controller 模块，保留所有平台的模拟事务测试；增加 port 生命周期断言，Watchdog 同样按平台/测试编译，不关闭 -D warnings。同时 checkout 升级固定 v6 SHA，消除 Node 20 退役提醒。第二轮远端结果待核对。

- 用户明确授权公开到 GitHub，并确认仓库 sing、MIT。只读核对实际账号 mmei0114 和现有仓库，创建新公开仓库 https://github.com/mmei0114/sing ，没有改动既有项目。
- 英文 README 重写为定位、真实预览、功能、快速开始、导航、边界与贡献入口；增加 README.zh-CN.md，原详细说明迁至 docs/usage.md。按照文档技能做独立无上下文读者审查，补全实际代理端口、Apply/Start、恢复命令、私有数据目录和手动核心获取说明。
- 新增 MIT 全文、CHANGELOG、CONTRIBUTING、SECURITY、隐私提醒问题模板、PR 模板、最小权限/固定 action SHA 的 macOS/Linux CI、源码预览版 release notes。没有新增客户端运行功能或切换用户网络；没有公开第三方 core 二进制。
- 新增可复现文档图与链接脚本；SVG 来自真实 ./sing --preview，已渲染成 PNG 检视，不含用户数据。YAML 解析、Markdown 本地链接、git diff --check、122 普通测试、fmt 与 Clippy 全目标通过。历史初筛未发现高置信度密钥/token 或私有状态路径；不将模式扫描宣称为绝对无泄漏保证。
- Superpowers/planning 技能路径仍缺失，继续三文件记录。GitHub 推送、topics、安全私报、远端 CI 和版本发布尚待下方完成记录，不把创建空仓库算作源码已发布。

## 2026-09-16 — sing 0.6.0 本地交付

- 完成 M2 主流程收尾：Native Source/SRS 的 local/remote 引用进入规则导入同一父事务；保留 source/binary 格式、核心加载与更新归属，不虚构规则数量。真实 core 验证本地 SRS 编译、绑定、加载；DNS 不随导入改变。订阅 Refresh Preview 只重取原来源、失败保留旧预览，规则 Change Source 重新取得 revision/上下文并保留输入和内联组，顺序变化要求重审位置。
- 运行时选节点由 API 写入后回读确认，不再改写原生默认成员或结构草稿；成功选择独立持久化为私有 selections.json。Start/Apply 恢复合法手动选择，显式默认成员修改优先，原生 cache_file 开启时让核心负责。恢复失败在 Overview/Activity 可见；API 不可用清空组缓存。隔离核心验证选择、记忆、Apply 恢复、默认值优先及鉴权失败无假成功/无记忆写入。
- 全局 Review Changes 新增对象/规则/DNS 摘要及 Native Diff，可独立切换、滚动、返回和显式 Apply。原始配置先比较再脱敏，密码单独变化也可见；末轮审查补上长值完整显示、absent/null 区分和终端控制转义回归。失败返回原审阅，不自动重复操作。
- 多行失败详情保留、未提交来源/UA/query 凭据在写日志前脱敏，Apply 错误直接报告实际恢复结果；原生 TUN 提示基于真实入站而非旧客户端模式字段。管理协议升级到 9；旧后台不会自动重启。
- 最终验证：122 项普通测试、9 项显式隔离/只读测试全部通过；cargo fmt --check、git diff --check、Clippy --all-targets -D warnings 与 release 构建通过。显式套件仅虚构临时数据/回环与模拟代理助手，macOS 仅做只读 inventory；公开下载核心/YouTube 测试仍跳过。Linux 未编译，真实接管与用户验收不计通过。
- 正式 release 80×24 离线 PTY 确认五工作区、可见按钮、Review → Native Diff → Back 及 q 恢复终端。./sing --version 为 0.6.0，Mach-O arm64；二进制 SHA-256：f219104212fe5a51061bbda5b95685044e3283cc2c2dfe64133e5c6bb0544a79。
- 构建前备份旧版至 .build/backups/v0.5.1-f70AYn2u/sing，仍报告 0.5.1，SHA-256：7604898e50ac68b76cda54fedddd0255df7d5efc24eef11c28bacc19ca0ef00a。源码基线 b476cdb 保留；本地交付节点见 Git，不推送、不纳入私有状态/凭据/二进制。
- README 已更新到当前正式工作流，新增 docs/acceptance-0.6.0.md 按 A01–A18 列明证据/范围。M2 代码阶段完成；M3 客户端设置引用等和 M4 真实 System/TUN/Linux/SSH、核心缓存异常与用户无指导验收仍未全部完成，长期目标不标完成。没有操作用户真实管理器、网络、订阅或迁移用户状态。指定辅助技能路径仍缺失，沿用三文件记录。

## 2026-09-16 — M2 导入恢复收尾 / M3 原生引用导航

- 订阅表单默认只展示名称与来源；可见 Advanced 展开 User-Agent，收起不会抹掉已填值，并显示 configured。Tab/Enter、F2、取消与返回预览沿用同一表单，空来源本地拦截；来源和 User-Agent 在视图中遮罩，按钮焦点的粘贴不会写进隐藏字段。
- 提交失败新增可滚动 Action Failed 详情；Enter/Esc 返回完整原表单/预览，不自动重复写入。失败原因过滤已知私密值、未提交来源与 User-Agent；修复脱敏快照 secret 为空时字符串替换污染整个错误的问题。后台轮询失败不反复弹窗。过期版本的重准备/合并仍待后续，不把保留输入等同完整冲突恢复。
- 新增 native/links.rs：出站/端点、入站、DNS、规则集分类型引用；覆盖分组成员/默认成员、路由与 DNS final、逻辑子规则、DNS bootstrap、拨号/下载路径及 TUN 地址规则集。区分服务器地址与 DNS tag，同名不同类型不会误报；原生规则集 tag 数组也参与定义查找。未知字段原样保留，本检查不取代选定核心的合法性校验，也不宣称覆盖所有未来扩展和客户端设置引用。
- 对象 More / Actions 中新增 References，显示 Uses / Used by；跳到对象所在既有页面，Back to References 恢复原工作区、筛选、列表位置和引用选择。长列表滚动、缺失引用、草稿变化后的旧索引保护已测试；没有添加第二套 DNS/入站编辑器。
- 删除被 Route/DNS 等使用的对象时前端直接打开引用列表；native::write 后端再次检查删除/改名是否留下悬空引用，失败时草稿与元数据不变。整份原生文档内同时更新定义和引用仍允许，已有其他未完成字段不妨碍编辑。删除前核对所选对象，避免并发重排后删除另一项。订阅更新的旧字符串扫描已共用类型化出站引用检查。
- 验证：114 项普通测试通过（新增 9 项），5 项普通套件默认忽略项未跑；6 项显式隔离集成全部通过，包含订阅首次连接、原生迁移/应用/回滚、规则模式、组切换、模拟系统代理恢复。cargo fmt --check、git diff --check、Clippy all-targets -D warnings 与 debug 构建通过。
- 54×18 / 80×24 / 140×40 的表单每个焦点、错误详情、250 条引用列表自动渲染通过；80×24 离线 PTY 实看订阅 Advanced 展开，以及组引用 → DNS 服务器 → Back to References → Esc → q 正常恢复终端。中途 PTY 因会话中断失效，重新打开离线 demo 完成后半段检查；不把此演练冒充真实用户无指导验收。
- A10 按上述自动及交互证据完成。总体 M2/M3/M4 尚未完成：SRS/remote 导入父事务、全局语义 Review、API 选择恢复、正式发布与真实 System/TUN/Linux/SSH/用户验收继续待办。release 仍为原 0.5.1，未替换用户当前版本、未启停用户管理器、未改系统代理或真实订阅。指定辅助技能路径缺失，继续既有三文件记录。

## 2026-09-15 — M2 接续：订阅连接引导与 Update All

- 实现 Subscriptions 中可见 Update All：逐来源获取并汇总新增/移除与警告，单次确认保存全部；任何下载/解析失败都不保存部分来源。单个/批量订阅预览新增 token/revision 校验，取消只影响自己的预览，保存失败仍保留预览。原生引用中的被移除节点明确报路径冲突，禁止提交悬空引用。
- 新增 subscriptions.rs：订阅预览可选 Save & Set Up / Save Only / Back to Source / Cancel，返回来源保留私密输入，视图仍遮罩凭据。保存后 Connection Setup 支持规则/全局/直连、当前组或节点目标、Keep / Proxy Ports / 本地 macOS System Proxy / TUN。Overview 有同一 Connection Setup 入口，可继续已保存成果；Network/Core 按钮进入既有编辑/安装流程，不复制配置。
- ConnectionSetupInfo 提供后台真实 revision，避免用脱敏快照计算错误版本；协议升级到 8。Review Setup 不持久写入、保存不启动；确认 Save Draft 后才另行显示 Start/Apply 审阅。首次生成原生配置需确认并私有备份，已有规则/DNS/复杂入站保留，TUN 模板与既有 Inbounds 共用；已有 TUN 不被 Ports/System 选择偷偷移除。
- SSH 不提供 System Proxy 并在后端拒绝该组合；TUN 明示授权与会话中断风险，未启用真实 TUN。离开已修改的引导时确认丢弃，已保存订阅保留。审阅返回原引导保留选择，后台错误恢复原对话框。
- 105 项普通测试通过（上一阶段 96 + 9），Clippy 全目标零警告、格式和 debug 构建通过；6 项显式隔离集成通过，包含新增的订阅保存→原生初始化预览→私有备份→保存但未启动→显式 Start→真实 gRPC 就绪→断开的端口流程；其余 5 项默认忽略测试未重跑。既有规则/模式/模拟系统代理恢复回归通过。
- 80×24 离线 PTY 验证可见 Connection Setup 按钮、目标选择、Review、Esc 返回保留选择、内存保存与正常退出。观察到保存预览复用了“Apply restarts”文案，已拆为独立保存摘要并新增断言；不把已停止内核说成会被当前保存动作重启。小屏订阅按钮及私密值不泄漏另有自动渲染测试。
- 测试仅使用临时目录、虚构订阅、回环服务及模拟系统代理助手；没有启停用户真实管理器、没有改真实网络。release 仍是既有 0.5.1，尚未替换用户使用版本；辅助技能路径仍缺失，沿用三文件记录。
- 后续仍有 M2 收尾（原生 SRS/remote 对象统一导入事务、User-Agent 渐进选项、错误恢复入口打磨）与 M3 引用跳转/全局语义 Review/运行选择恢复；真实 System/TUN/Linux/SSH 与用户无指导可用性待验收。此次新增端口流程通过不等于 A04/A08 或整体目标全部完成。

## 2026-09-15 — M2 进行中：一次建组与规则导入父事务

- 恢复中断处并修复语法/入口回归；指定 Superpowers 与 planning-with-files 路径仍不存在，沿用现有三文件记录。未重置工作树、未改现有主导航设计。
- 新增 group.rs 单工作表：显示名与随机稳定 tag 分离；Manual/Automatic、按名称/来源搜索、多选、合法默认成员、组/端点引用、按需测试参数；统一 Create/Save 一次提交。未知原生字段保留；重复 tag、端点冲突、空组、无效默认值、缺失成员、循环与过期 revision 均拒绝。
- 新增 rule_import.rs：来源/格式预览、Send Matching Traffic To、真实插入位置、可检测的前置 catch-all 提示、转换报告、内联 New Group。内联 Use Group 仅暂存；最终 Save 将组、规则资源、路由作为一次持久提交，DNS 不改变。排除项必须先打开转换报告，外部策略名不执行。
- 新增独立的原生 JSON 规则路径：保留整份来源文档元数据，原生 rules 原样进入 inline rule-set，复合条件及未知字段不被普通列表转换器扁平化。刷新同样保留语义并阻止覆盖本地编辑；配置合法性仍由选定内核在 Check/Apply 验证。二进制 SRS 仍走已有 Add Native Rule Set 的原生 local/remote 编辑入口，尚未统一进此父事务。
- 后台协议提升到 7；预览 token + 草稿 revision 双重检查；取消只清理对应 token，不清除其他窗口较新的预览。保存失败保留预览/输入；子窗取消返回父窗，父窗取消不留下组。新增 display_names 和可选 native_document 元数据，不替换原生配置事实来源。
- 额外修复：清空已有组后仍提示未保存；Automatic 自动展开测试参数，Manual 不显示无效测试参数；可见 Select Member 按钮转入内容选择，避免焦点 Enter 重复激活自身。
- 验证：96 项普通测试通过（原 M1 78 + 18 项），cargo fmt --check / git diff --check / Clippy all-targets -D warnings / debug 构建通过；已通过 80×24 --demo PTY 中手动组/自动组一次保存、草稿提示及 q 恢复终端；54×18、80×24、140×40 的规则绑定和 240+ Unicode 名称组列表自动渲染通过。没有用自动测试冒充新手可发现性测试。
- 真实 sing-box 1.14.0 隔离测试通过：新组+规则绑定失败时全草稿不变，成功后核心校验通过；复合原生 JSON 规则保真、DNS 模板、gRPC 选择、并发应用拒绝与回滚回归通过。沙箱首次禁止 socket 导致测试退出，获准后仅在独立临时目录/回环端口重跑通过；未启用系统代理/TUN，未使用用户订阅或重启用户管理器。
- 补跑 tests/manager.rs 两项及 tests/rules_flow.rs 两项均通过：回环订阅预览/取消/刷新、模拟系统代理恢复顺序、规则/全局/直连实际回环分流、连接关闭及旧规则刷新兼容。加上 native_flow，共 5 项显式隔离集成通过；其余 5 项默认忽略测试本阶段未重跑，不算通过。
- 当前为源码/debug 阶段，release 仍为既有 0.5.1，./sing 优先旧 release；未发布未完成的升级。M2 尚缺 F1 保存后选出口/接管/Review & Start 的连贯引导与 Update All，M3 的引用跳转、语义 Review、运行选择恢复尚待实现，M4 平台与用户实测未完成。A05 有阶段证据；A06 仍按完整范围待验收。整体目标不标完成。

## 2026-09-15 — M1 正式代码：五工作区与可见操作

- 新增 src/ui/navigation.rs，将五个产品工作区映射到既有唯一原生编辑器；Settings 为全局入口，DNS/Capture/Inbounds 和 Rules/Rule Sets 归属按最终规格落实。Proxies 分组、节点、全部出站视图保留原数组索引，不复制对象。
- 新增可聚焦全局/页面按钮、主/子导航焦点、Review 焦点；Tab/Shift+Tab、方向键与 Enter 可执行主要入口；输入框数字与导航隔离；表单新增可聚焦 Review Import / Save Draft / Cancel；高级低频操作收进可搜索 Actions/More。
- 紧凑顶栏、宽屏列表/详情、窄屏进入详情；顶部状态含接管与实际/保存模式；Routing 默认目标可见。Configure TUN 单个时打开既有入站、多个时转入筛选列表；不存在才走既有原生新增模板，不重复维护配置。
- 验证：78 项普通测试通过（原 73 + 5 项映射/按钮键盘/对象索引/搜索跳转/TUN 唯一对象测试），10 项显式外部/回环/系统测试仍忽略，未冒充运行；cargo fmt、Clippy all-targets 零警告通过，debug 构建通过。使用工作区 .build/cargo 依赖缓存离线构建；默认 Cargo 缓存不足的尝试已停止，无额外下载。
- 80×24 PTY 使用 --demo 检查 Overview、Proxies、Tab 聚焦导入与表单按钮、Network → DNS 和 q 恢复终端。发现表单按钮覆盖旧提示残留，已清除背景并通过后续回归。没有操作真实订阅或后台，没有重建 release，用户 ./sing 仍优先旧 release。
- M1/A01 完成；M2–M4 仍未完成。尤其组创建仍沿用原生表单+成员子窗两层编辑，规则导入尚未内联建组/选择插入位置，Update All/准确语义 Review/选择恢复/用户实测等尚待实施，不宣称本轮产品已交付。

## 2026-09-15 — 最终产品规格与目标固化

- 根据用户最终讨论新增 docs/product-spec-v1.md，固定信息架构、熟悉入口、唯一对象编辑器、三条端到端流程、保存/应用语义、开发边界、M0–M4 和 A01–A18 完成标准。
- 更新 task_plan.md 当前任务，并标明旧研究六页导航/独立原型已失效；历史研究保留不删除。当前工作树原有研究记录保留。
- 本轮使用文档协作技能归纳已有上下文并检查可执行性；指定 bootstrap/planning 技能路径仍缺失。仅编辑文档，不更改 UI/用户数据/网络，不把创建目标等同实现完成。
- 已创建产品内 active 实施目标，完整引用本规格和阶段/验收边界。独立读者正确复述导航、导入、唯一编辑器、保存/应用和完成标准，提出三处细节：内联取消层级、运行选择持久性、模式覆盖映射；已补入明确规则。用户可用性测试尚未进行。
- 独立读者复核确认三项文档歧义已消除；文档空白检查无报错。此项是规格可读性检查，不是客户端/网络/新手实测通过。

## 2026-09-15 — 产品交互研究与方向提案

- 只读检查现有界面/审阅逻辑，收集官方代理客户端及优秀 TUI 的文档、源码、公开演示与截图；没有连接用户核心到公开 Dashboard，没有输入订阅或改动主机代理。
- 新增 docs/product-research-2026-09-15.md：材料索引、四组公开视觉参考、现状问题、六页信息架构、三条完整任务流程、TUI 交互规范、两张文字线框和验收目标。
- 指定 Superpowers 和 planning-with-files 路径仍缺失，沿用现有三文件记录。研究阶段没有修改源代码、运行数据或创建生产 UI；没有运行代码测试，也不把历史测试结果视为新方案验收。
- 研究提案完成；等待用户反馈后再做离线交互原型，不直接进行下一次生产导航重排。

## 2026-09-15 — 0.5.1 顶部导航与就地提示

- 按用户反馈移除侧栏，恢复顶部数字标签，宽度不足时换行；Tab/左右切页和数字直达可用，页面内容不因导航焦点消失。
- Advanced 仅显示无专页的原生部分，不再重复 DNS、Routing、Inbounds、Outbounds；完整 JSON 编辑仍可用，数据未变。
- 各页面增加精简操作行；Outbounds g 明确建立手动/自动组，Resources 显示订阅导入与 QX/Clash 转换快捷键，表单/成员选择保存与审阅提示更正。
- 73 项普通测试通过，新增顶部导航/建组入口、Advanced 排除重复但保留完整编辑、54×18/80×24/120×35 提示可见性回归。Clippy 全目标零警告通过。离线 PTY 验证 3→g 建组入口、6→]→C 转换表单、7 高级列表及 q 终端恢复。
- 未操作真实管理器、订阅或网络；本轮不重跑需要内核/回环监听的集成测试（无底层改动）。辅助技能路径仍缺失，已向用户说明，继续三文件记录。
- 最终 release 构建完成，./sing --version 确认为 0.5.1；末次修改后普通测试 73 项、格式检查及 Clippy 重新通过。0.5.0 升级仅需退出并重开 TUI，后台可保持运行。

## 2026-09-15 — 0.5.0 原生配置工作区

- 用户确认后先启用 Git：基线 d45b9be、标签 v0.4.1-baseline，工作分支 native-config-redesign；独立可运行备份 .build/backups/v0.4.1/sing，SHA-256 为 e627e3c8d84d7823441aa28cd1d702eefb4fe818c0151c28f58ae8a5d64529ea。源码 Git 不含私有运行数据、订阅、构建或日志，没有配置远端或推送。
- 原生 JSON 成为配置事实来源，表单只修改变更字段；未知字段和顺序保留，任意原生部分可经高级 JSON 编辑。schema 2 迁移必须显式审阅确认，确认前先私有备份旧 state；本轮没有迁移用户真实数据。
- 导航改为 Overview、Configuration（Inbounds / Outbounds / Routing / DNS / Resources / Advanced）、Activity（Connections / Logs / Diagnostics）、Settings。英文单语言、上下文帮助、窄终端导航、列表/详情；节点名保持原语言。
- DNS 拆为 Resolvers / Rules / Options；路由和解析不再隐式互改。原生分组支持嵌套，规则保持有序，转换规则资源与节点订阅只更新所属对象，局部冲突阻止覆盖。原生远程规则保留内核更新机制。
- F2/Ctrl+S 保存草稿，A 审阅后应用，版本检查拦截过期编辑/确认。应用先内核校验，失败尝试恢复之前运行配置；管理 API 本地接入点受保护，TUN 从实际原生入站确定，与 macOS 系统代理独立。
- 70 项普通测试、8 项显式隔离/只读测试通过；Clippy 全目标零警告、格式检查通过。真实 1.14.0 校验八类 DNS 模板与自定义 DoH 参数，覆盖迁移备份、脱敏、规则转换不改 DNS、嵌套选择器、应用冲突、回滚及既有本地 HTTP/模拟系统代理生命周期。
- 离线 PTY 已验证英文导航、新增 DoH 解析器并保存、可读节点选择器及正常退出；54×18、80×24、120×35 自动渲染回归通过。另加草稿 TUN 不冒充已运行状态、成员顺序和取消编辑回归；连接观察标记采样时间和过期。
- 真实用户管理器、系统代理、DNS、TUN 均未操作；不宣称网速已改善。Linux/TUN/SSH 真机、DNS 计时诊断、完整导入辅助、自动更新和后台服务加固仍待后续。指定 Superpowers / planning-with-files 路径缺失，已说明并按三文件记录继续。
- 最终 release 构建通过，入口 ./sing --version 为 0.5.0，旧备份仍为 0.4.1 且摘要一致。release 离线 PTY 复核窄屏导航至 DNS 和 q 正常恢复终端；用户须主动 shutdown 旧后台后打开新协议 6 工作区。

## 2026-09-15 全局方向讨论

- 完成现有模型、生成器、入站/出站/规则资源关系及八页导航的只读审视，核对官方 1.14 原生路由、规则集、DNS、TUN 和平台限制。
- 已整理候选方向供用户确认：原生配置保真；配置对象与运行观察分层；英文单语言；简洁常态界面与按需帮助；先基线备份再分阶段升级。
- 仅更新三文件记录；未修改程序、用户配置、真实网络，未初始化 Git、创建备份或运行性能测试。方案尚待用户确认。

## 0.4.1 用户反馈修复

- 定位实际日志后修复 legacy DNS 在 Rule 默认直连时的无效 detour；未改用户策略、订阅或真实网络。
- 增加只读取本次启动日志的脱敏错误摘要；主页区分全局组/规则未匹配目标，组内节点使用实时 API 选择。
- M/分层设置选项、连接详情/关闭确认、D诊断的中英文独立；显示文本与内部配置值解耦。协议5要求用户主动重启旧后台才能使用修复。
- 单测先确认旧生成器失败，再修复通过；70项普通测试、7项显式本地/只读回归、Clippy零警告、格式检查及release通过。PTY确认0.4.1，M英中文互不并排、L切换后中文D诊断和正常退出。实际用户TUN未启停；核心故障组合以隔离端口模式验证。

## 0.4 实现（中断后继续）

- 已完成独立 rule/global/direct、全局目标、内网例外、旧配置兼容及模式覆盖的 DNS；不改已有保存的规则。直连不加载代理节点/自动测速组，不需要订阅。
- 原生 Connections/CloseConnection API、活跃/最近关闭切换、过滤、实际路由/进程详情、固定目标关闭确认、采样过期和错误提示已接入。配置按接管/路由/DNS/高级分层，M快速路由、D本地诊断；管理协议4。
- 最终66项普通测试和7项显式本地/只读测试通过，Clippy零警告、格式检查及release构建通过；新增真实核心端到端验证三模式、单连接关闭和完整策略回滚。PTY验证路由保存、连接详情与关闭确认取消、D诊断、q恢复终端；修复诊断正文覆盖快捷键底栏，宽/窄终端回归通过。./sing --version为0.4.0。未使用私人订阅、未重启用户后台、未更改系统代理。
- 用户指定 Superpowers 和 planning-with-files 路径再次核对仍缺失；继续使用现有三文件记录。

## 0.3 实现（中断后继续）

- 已实现分组、规则资源与独立绑定模型；旧数据兼容，旧 DNS 保留 legacy，新实例 paired。
- 本地严格转换 QX / Clash payload / 域名 / CIDR / 原生简单 JSON 子集。未知或复合条件整条警告，不剥离限制；无可用规则不覆盖旧资源。
- 后台已接入预览 / 取消 / 保存 / 更新差异、绑定启停调序、引用完整性检查和原生 gRPC 分组切换。管理协议升至 3，要求用户主动退出旧后台后升级。
- TUI 新增 6 Groups / 7 Rules、节点勾选及搜索、可读目标名、固定可见兼容警告、DNS 设置；修复断开时 system 模式误报未验证。
- 普通测试 57 项通过，Clippy 通过。真实核心的规则配置 / paired DNS / urltest 校验 / 手动组切换，以及旧核心生命周期和模拟系统代理恢复回归通过。正在补充真实 HTTP 分流和最终 release / PTY 验证。
- 用户公开 YouTube URL 的直连下载测试失败（当前环境请求失败）；不把它计作在线转换验收。未使用私人订阅、未改主机网络或重启用户旧管理器。
- 最终 57 项普通测试及 6 项显式回环 / 只读测试通过；额外实际 HTTP 请求按导入规则进入本地模拟节点 A，再经原生组切换进入 B，默认直连无法假冒该结果。测试助手初次遇 macOS 接受 socket 继承非阻塞的问题，修正模拟器后通过。
- Clippy 全目标零警告、格式检查和 release 构建通过，`./sing --version` 确认为 0.3.0。离线 PTY 验证七页导航、分组成员搜索 / 勾选 / 保存、规则目标表单与取消、q 退出恢复终端。
- README 已补充升级步骤、分组到规则的入门路径、兼容警告、DNS 适用范围、显式测试和未验收事项。交付无需真实订阅凭据。

## 用户澄清：分类远程规则订阅

- 已打开实际 YouTube.list，发现通配符和 USER-AGENT，说明即便是服务分类清单也需要兼容性预览。
- 核对 sing-box headless rule / rule-set、Mihomo provider 与 routing、QX 官方样例；回应其分组绑定路径符合常见架构，同时区分配置管理和运行时路径。
- 只记录建议，未更改代码、已有订阅、分组或网络设置；指定辅助技能缺失，继续三文件记录。

## 分组 / 规则 / DNS 讨论

- 阅读当前模型，确认尚无自定义组与外部规则集管理；现有进程功能仅简单 process_name 字段，未完成 App 选择器与实际命中验收。
- 对照 sing-box、Mihomo、QX 官方资料核实可转换子集、策略覆盖风险、进程接管前提与 DNS 协作边界。
- 使用简洁的静态关系图解释接管、规则、分组、节点的层次，未创建应用页面或实施网络设置。
- 本轮仅记录设计建议；不宣称已经支持任意 Clash/QX 转换，不宣称 DNS/分流可保证解决已有性能问题。指定辅助技能路径仍缺失，继续既有三文件记录。

## 0.2 用户反馈诊断

- 只读检查当前 manager 状态与 macOS 有效代理，确认检查时已经由用户断开、设置已恢复；没有擅自重新连接或测速。
- 定位到未运行时 Overview 仍使用 system 模式警告的显示问题；未按诊断请求擅自实施修改。
- 确认无自定义分流规则，并询问 QX 对比环境、具体节点与慢的类型。尚不能判断性能瓶颈。
- 指定 Superpowers / planning-with-files 路径再次检查仍不存在，沿用三文件记录。

## 2026-09-14 系统代理实现

- 中断后继续原工作，没有重建用户数据，也没有使用真实订阅测试。
- 新增可注入系统代理事务、持久写前恢复记录、原生 macOS SystemConfiguration 后端、显式 sudo 助手与独立心跳监护。
- 接入 system 模式、核心就绪后才启用、恢复成功后才停止、只恢复 R / --restore-system-proxy、显式 v HTTPS 检查；旧后台协议检测要求用户主动升级。
- 界面区分 PORT READY / SYSTEM PROXY ON / ROUTING UNVERIFIED / PROXY RECOVERY，网络访问检查独立显示；54×18 中保留确认和取消操作。
- 真实 sing-box 配合模拟助手测试通过：启用和恢复顺序正确；注入恢复失败后，disconnect 和 shutdown 均不停止核心或管理器；只恢复、重新启用、部分启用失败回滚均通过。
- 既有真实核心/gRPC/选择器/本地 HTTP/回滚及后台订阅测试通过；新增检查器的 204 / 200 / 302 本地响应测试通过，不访问公开目标。
- 原生系统接口只读测试通过（5 个启用的物理类服务），无 root helper 启动，无系统代理 / DNS / 路由写入。
- README 已补充 0.2 上手和升级、恢复入口、私有路径和特权监护限制。
- 最终：44 项普通测试、5 项显式回环 / 原生只读测试通过；Clippy all-targets -D warnings 通过。release sing 0.2.0 已构建（macOS arm64，约 6.8 MiB），入口 ./sing --version 确认。
- 终端 smoke：离线 demo 的状态行、设置表单 port → system、取消和 q 恢复终端通过。恢复失败的 CLI shutdown 返回非零退出码，模拟集成复测通过。
- 本轮没有启动 root helper、没有改系统代理 / DNS / 路由，也没有重启用户旧后台。尚待用户确认后的真实授权与浏览器验收；Linux / TUN 真机验收仍未做。

## sing 改名与只读诊断

- 用户要求统一名称为 sing；已改 Cargo 包、可执行入口、UI 标题、User-Agent、测试环境变量和使用文档。
- 对既有 sbtui 目录做读取兼容，不移动正在使用的管理 socket，不复制私人订阅，不重启核心。全新安装使用 sing 目录。
- 只读检测当前核心已断开、保存模式 port、macOS 未启用系统代理；不能据此声称节点本身已通过网页测试。
- 原用户指定 Superpowers / planning-with-files 文件仍不存在；按三文件记录继续。
- 28 项普通测试通过，包含新增的旧数据目录兼容和新安装命名测试；Clippy 全目标零警告；release 命令 sing 0.1.0 已构建。
- 生命周期日志最后为 Disconnected by user，不是核心崩溃。已保存代理端口 2080；未进行真实节点网页请求，也未启用系统代理。

## 2026-09-13

- 阅读用户对新 TUI 和旧项目关系的约束。
- 检查工作区：空目录，无 Git 仓库。
- 查阅 LazyGit、K9s、btop、Television 的官方资料。
- 发起两项非阻塞偏好确认：Linux/SSH 范围、默认界面语言。
- 用户已回复：覆盖 Mac 本机 + Linux 本机及 SSH；英文优先，可切换中文。已同步到设计。
- 写入 docs/product-design-v0.1.md，包含首次连接、主界面、订阅、配置辅助、错误状态、平台差异和产品验收标准。
- 本轮仅创建设计文档；没有安装核心或修改系统代理、DNS、路由、服务。

验证方式：文档结构与交互一致性检查；原型、核心 API 和平台行为均尚未运行验证，设计目标不代表已实现能力。

检查结果：已同步确认范围和语言偏好；统一页面导航为 Overview / Nodes / Subscriptions / Config / Activity，避免 Config 与应用 Settings 菜单混淆。

## 实现轮次

- 用户授权构建；明确不需要提交真实订阅，使用虚构 fixtures 和本地 HTTP 服务。
- Rust 1.94 + Ratatui，6 个模块：存储、订阅、配置、gRPC、后台管理、TUI。
- 实现独立 Unix socket 管理进程；UI 退出不终止核心；支持 shutdown、节点选择、测速、配置校验、应用失败恢复。
- 实现官方 1.14.0 核心下载。初始 GitHub API 摘要查询遇 403 限流，改为从官方发布页核实并固定四种平台的 SHA-256；下载与版本验证通过。
- 真实 macOS arm64 sing-box 1.14.0 测试通过：gRPC、selector、本地 HTTP 代理、错误导入保留数据、端口冲突回滚、断开。
- 独立后台管理器测试通过：本地 HTTP 订阅下载、预览、取消、确认、脱敏、刷新失败保留数据、客户端断开后核心持续运行、核心日志、清理。
- PTY 操作已验证：粘贴假节点、保存预览、节点搜索、JSON 配置预览。
- 检测当前 Homebrew sing-box 1.13.2；没有修改它。测试核心只放在工作区 .build/test-core。
- 没有启用 TUN，没有更改主机代理、DNS 或路由。TUN helper 已实现但标记为实验、未启用验证。
- 补充 README、三个虚构订阅格式 fixture、便捷启动脚本。
- release 构建成功：target/release/sbtui，0.1.0，macOS arm64，约 6.5 MiB。
- 最终普通测试 26 项通过；3 项需网络 / loopback 的测试分别显式运行通过，不混入普通测试。Clippy all-targets -D warnings 通过。
- PTY 验证了中文切换与 q 正常恢复终端。隔离的 ui-smoke 后台管理器已通过 --shutdown 停止；保留虚构测试文件以便复查。
- 已完成第一版可运行交付；README 如实记录范围和未验收的平台 / 特权行为。
